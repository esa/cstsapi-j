/**
 * This class file was automatically generated by jASN1 v1.8.1 (http://www.openmuc.org)
 */

package ccsds.csts.common.types;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class EventValue implements Serializable {

	private static final long serialVersionUID = 1L;

	public byte[] code = null;
	private SequenceOfQualifiedValues qualifiedValues = null;
	private BerNull empty = null;
	private Embedded eventValueExtension = null;
	
	public EventValue() {
	}

	public EventValue(byte[] code) {
		this.code = code;
	}

	public void setQualifiedValues(SequenceOfQualifiedValues qualifiedValues) {
		this.qualifiedValues = qualifiedValues;
	}

	public SequenceOfQualifiedValues getQualifiedValues() {
		return qualifiedValues;
	}

	public void setEmpty(BerNull empty) {
		this.empty = empty;
	}

	public BerNull getEmpty() {
		return empty;
	}

	public void setEventValueExtension(Embedded eventValueExtension) {
		this.eventValueExtension = eventValueExtension;
	}

	public Embedded getEventValueExtension() {
		return eventValueExtension;
	}

	public int encode(BerByteArrayOutputStream os) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				os.write(code[i]);
			}
			return code.length;
		}

		int codeLength = 0;
		if (eventValueExtension != null) {
			codeLength += eventValueExtension.encode(os, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 100
			os.write(0x64);
			os.write(0xBF);
			codeLength += 2;
			return codeLength;
		}
		
		if (empty != null) {
			codeLength += empty.encode(os, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			os.write(0x82);
			codeLength += 1;
			return codeLength;
		}
		
		if (qualifiedValues != null) {
			codeLength += qualifiedValues.encode(os, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
			os.write(0xA1);
			codeLength += 1;
			return codeLength;
		}
		
		throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
	}

	public int decode(InputStream is) throws IOException {
		return decode(is, null);
	}

	public int decode(InputStream is, BerTag berTag) throws IOException {

		int codeLength = 0;
		BerTag passedTag = berTag;

		if (berTag == null) {
			berTag = new BerTag();
			codeLength += berTag.decode(is);
		}

		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
			qualifiedValues = new SequenceOfQualifiedValues();
			codeLength += qualifiedValues.decode(is, false);
			return codeLength;
		}

		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			empty = new BerNull();
			codeLength += empty.decode(is, false);
			return codeLength;
		}

		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 100)) {
			eventValueExtension = new Embedded();
			codeLength += eventValueExtension.decode(is, false);
			return codeLength;
		}

		if (passedTag != null) {
			return 0;
		}

		throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		BerByteArrayOutputStream os = new BerByteArrayOutputStream(encodingSizeGuess);
		encode(os);
		code = os.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		if (qualifiedValues != null) {
			sb.append("qualifiedValues: ");
			qualifiedValues.appendAsString(sb, indentLevel + 1);
			return;
		}

		if (empty != null) {
			sb.append("empty: ").append(empty);
			return;
		}

		if (eventValueExtension != null) {
			sb.append("eventValueExtension: ");
			eventValueExtension.appendAsString(sb, indentLevel + 1);
			return;
		}

		sb.append("<none>");
	}

}

