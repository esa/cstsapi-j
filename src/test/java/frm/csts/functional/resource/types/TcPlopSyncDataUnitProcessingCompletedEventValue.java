/**
 * This class file was automatically generated by jASN1 v1.11.3-SNAPSHOT (http://www.beanit.com)
 */

package frm.csts.functional.resource.types;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import com.beanit.jasn1.ber.*;
import com.beanit.jasn1.ber.types.*;
import com.beanit.jasn1.ber.types.string.*;


public class TcPlopSyncDataUnitProcessingCompletedEventValue implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static class ServiceInstanceIdentifier implements BerType, Serializable {

		private static final long serialVersionUID = 1L;

		public byte[] code = null;
		private SleSvcInstanceId sleServiceInstance = null;
		private CstsSvcInstanceId cstsServiceInstance = null;
		
		public ServiceInstanceIdentifier() {
		}

		public ServiceInstanceIdentifier(byte[] code) {
			this.code = code;
		}

		public void setSleServiceInstance(SleSvcInstanceId sleServiceInstance) {
			this.sleServiceInstance = sleServiceInstance;
		}

		public SleSvcInstanceId getSleServiceInstance() {
			return sleServiceInstance;
		}

		public void setCstsServiceInstance(CstsSvcInstanceId cstsServiceInstance) {
			this.cstsServiceInstance = cstsServiceInstance;
		}

		public CstsSvcInstanceId getCstsServiceInstance() {
			return cstsServiceInstance;
		}

		public int encode(OutputStream reverseOS) throws IOException {

			if (code != null) {
				for (int i = code.length - 1; i >= 0; i--) {
					reverseOS.write(code[i]);
				}
				return code.length;
			}

			int codeLength = 0;
			if (cstsServiceInstance != null) {
				codeLength += cstsServiceInstance.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
				reverseOS.write(0xA1);
				codeLength += 1;
				return codeLength;
			}
			
			if (sleServiceInstance != null) {
				codeLength += sleServiceInstance.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 0
				reverseOS.write(0x80);
				codeLength += 1;
				return codeLength;
			}
			
			throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
		}

		public int decode(InputStream is) throws IOException {
			return decode(is, null);
		}

		public int decode(InputStream is, BerTag berTag) throws IOException {

			int codeLength = 0;
			BerTag passedTag = berTag;

			if (berTag == null) {
				berTag = new BerTag();
				codeLength += berTag.decode(is);
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
				sleServiceInstance = new SleSvcInstanceId();
				codeLength += sleServiceInstance.decode(is, false);
				return codeLength;
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
				cstsServiceInstance = new CstsSvcInstanceId();
				codeLength += cstsServiceInstance.decode(is, false);
				return codeLength;
			}

			if (passedTag != null) {
				return 0;
			}

			throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
		}

		public void encodeAndSave(int encodingSizeGuess) throws IOException {
			ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
			encode(reverseOS);
			code = reverseOS.getArray();
		}

		public String toString() {
			StringBuilder sb = new StringBuilder();
			appendAsString(sb, 0);
			return sb.toString();
		}

		public void appendAsString(StringBuilder sb, int indentLevel) {

			if (sleServiceInstance != null) {
				sb.append("sleServiceInstance: ").append(sleServiceInstance);
				return;
			}

			if (cstsServiceInstance != null) {
				sb.append("cstsServiceInstance: ");
				cstsServiceInstance.appendAsString(sb, indentLevel + 1);
				return;
			}

			sb.append("<none>");
		}

	}

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerInteger dataUnitId = null;
	private ServiceInstanceIdentifier serviceInstanceIdentifier = null;
	
	public TcPlopSyncDataUnitProcessingCompletedEventValue() {
	}

	public TcPlopSyncDataUnitProcessingCompletedEventValue(byte[] code) {
		this.code = code;
	}

	public void setDataUnitId(BerInteger dataUnitId) {
		this.dataUnitId = dataUnitId;
	}

	public BerInteger getDataUnitId() {
		return dataUnitId;
	}

	public void setServiceInstanceIdentifier(ServiceInstanceIdentifier serviceInstanceIdentifier) {
		this.serviceInstanceIdentifier = serviceInstanceIdentifier;
	}

	public ServiceInstanceIdentifier getServiceInstanceIdentifier() {
		return serviceInstanceIdentifier;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		codeLength += serviceInstanceIdentifier.encode(reverseOS);
		
		codeLength += dataUnitId.encode(reverseOS, true);
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerInteger.tag)) {
			dataUnitId = new BerInteger();
			subCodeLength += dataUnitId.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		else {
			throw new IOException("Tag does not match the mandatory sequence element tag.");
		}
		
		serviceInstanceIdentifier = new ServiceInstanceIdentifier();
		subCodeLength += serviceInstanceIdentifier.decode(is, berTag);
		if (subCodeLength == totalLength) {
			return codeLength;
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		sb.append("\n");
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (dataUnitId != null) {
			sb.append("dataUnitId: ").append(dataUnitId);
		}
		else {
			sb.append("dataUnitId: <empty-required-field>");
		}
		
		sb.append(",\n");
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (serviceInstanceIdentifier != null) {
			sb.append("serviceInstanceIdentifier: ");
			serviceInstanceIdentifier.appendAsString(sb, indentLevel + 1);
		}
		else {
			sb.append("serviceInstanceIdentifier: <empty-required-field>");
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

